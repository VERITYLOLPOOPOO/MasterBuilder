const form=document.querySelector('#calc');const total=document.querySelector('#total');function calculate(){let price=Number(document.querySelector('#shipping').value);document.querySelectorAll('#calc input[type="checkbox"]:checked').forEach(item=>price+=Number(item.value));total.textContent=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(price)}form.addEventListener('change',calculate);calculate();